import Image from "next/image";
import Background from "../../../public/background.png";
import { HomeImage } from "./Home.css";
import { Poppins } from "next/font/google";

const poppins = Poppins({
  weight: "600",
  subsets: ["devanagari"],
});

export default function HomeComponent() {
  return (
    <section>
    </section>
  );
}
